package com.example.demo.repository;

import com.example.demo.model.ServiceSchema;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ServiceSchemaRepository extends JpaRepository<ServiceSchema, Long> {
}
